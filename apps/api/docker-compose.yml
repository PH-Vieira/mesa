services:
  api:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
    image: mesa-api:latest
    container_name: mesa-api
    restart: unless-stopped
    ports:
      - "3333:3333"
    env_file:
      - .env
    volumes:
      - ./data:/app/apps/api/data
